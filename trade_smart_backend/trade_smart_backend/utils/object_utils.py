def json_schema_validator(a,b):
    return {'success': True}

# def nested_path_get(obj, path_str, strict=False, mode = 'GET', default_return_value=None):
#     nested_keys = path_str.split(".")
#     for index, key in enumerate(nested_keys):
#       pass
